# Date from ISO8601

An MS Office Add-in for Excel that provides a User Defined Function (UDF) to
convert an ISO 8601 formatted timestamp into an Excel date/time value.

Optionally, it can convert the date to a different time zone.
